# Version control

Sync this project with **GitHub Desktop**. See **[GITHUB_DESKTOP.md](GITHUB_DESKTOP.md)** for setup.

- **Ignored:** `.env`, training images, `*.pt`, caches, `analytics.db` (see `.gitignore`). Only code and config are committed.
