{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum by (model, reason) (rate(hermes_router_requests_total[5m]))",
          "legendFormat": "{{model}} / {{reason}}",
          "refId": "A"
        }
      ],
      "title": "Router request rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "sum(rate(hermes_router_request_duration_seconds_sum[5m])) / sum(rate(hermes_router_request_duration_seconds_count[5m]))",
          "legendFormat": "avg latency",
          "refId": "A"
        }
      ],
      "title": "Router avg latency",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "max": 1, "min": 0 }, "overrides": [] },
      "gridPos": { "h": 4, "w": 6, "x": 0, "y": 8 },
      "id": 3,
      "targets": [{ "expr": "hermes_router_up", "refId": "A" }],
      "title": "Router up",
      "type": "gauge"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 12 },
      "id": 4,
      "targets": [
        { "expr": "ollama_models_loaded", "legendFormat": "models loaded", "refId": "A" }
      ],
      "title": "Ollama models in VRAM",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "decgbytes" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 12 },
      "id": 5,
      "targets": [
        { "expr": "ollama_vram_bytes", "legendFormat": "total VRAM", "refId": "A" }
      ],
      "title": "Ollama VRAM usage",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["hermes"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "title": "Hermes Stack Overview",
  "uid": "hermes-stack-overview",
  "version": 1
}
